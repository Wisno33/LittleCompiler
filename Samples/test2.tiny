PROGRAM p
BEGIN

	INT a,b,c,d;
	
	FUNCTION VOID main()
	BEGIN
		a := 20;
		b := 30;
		c := 40;

		c := a - b;
		b := b*b;
		b := b + a;
		d := (b*a);
		a := d/a;

		WRITE (c);
		WRITE (b);
		WRITE (a);
	
	END
END
