PROGRAM expr
BEGIN

	INT a,b,c,d,e,f;
	STRING newline := "\n";

	FUNCTION VOID main()
	BEGIN
		a := 1;
		b := 2;

          READ(c, d);

          e := a * c;
          f := b * d;
	  e := e + f;

          WRITE(c, newline, d, newline, e, newline);

	END	
END
